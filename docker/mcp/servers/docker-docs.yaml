server:
  id: docker-docs
  displayName: Docker Docs
  source: peer
  kind: peer
  trustClass: external-read
  mutationLevel: read
  dockerRuntime:
    applyMode: catalog
    catalogServerId: docker-docs
  tools:
    - toolId: docker.docs.search
      displayName: Search Docker Docs
      category: docs-search
      trustClass: external-read
      mutationLevel: read
      semanticCapabilityId: docs.search.docker
