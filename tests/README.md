# tests

End-to-end regression coverage lives in `tests/e2e`.

Current focus:

- service-layer trust boundaries
- corpus separation
- deterministic promotion and snapshot behavior
- chunking regressions
- bounded retrieval packet behavior
